 // Re-export the canonical useSettings hook from the top-level implementation.
 // Consumers can import from '@/hooks/useSettings' (directory) and get a single canonical export.
 export { useSettings } from '../useSettings';
